# Deterministic installs (CMD-001 `install`, ADR-0001).
save-exact=true
engine-strict=true
# pnpm's default isolated node_modules already prevents phantom dependencies,
# which is what protects the /packages/domain purity rule (ADR-0001).
# Hoisting is left at the default: disabling it entirely breaks bundlers that
# resolve their own peer graph.
