{
  "name": "@northstar/auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src",
    "test": "vitest run",
    "test:authz": "vitest run --reporter=verbose",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@northstar/domain": "workspace:*"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "4.1.11"
  }
}
